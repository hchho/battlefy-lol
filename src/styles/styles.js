const styles = theme => ({
  root: {
    display: 'flex',
    alignContent: 'center',
    alignItems: 'center',
    flexDirection: 'column',
  },
  formControl: {
    display: 'flex',
  },
  button: {
    margin: theme.spacing.unit,
  },
});

export default styles
